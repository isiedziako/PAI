liczba = 2
while(liczba <= 5)
{
    document.write(liczba + "<br>")
    liczba++
}

for(liczba = 12; liczba <= 16; liczba++)
{
    document.write(liczba + "<br>")  
document.write("<br>") 
}
liczba = 4
while(liczba <= 12)
{
    if(liczba % 2 == 0)
    {
        document.write(liczba + "<br>")
     }
     liczba++
}

for(liczba = 0; liczba < 4; liczba++)
{
     document.write("Liczba <br>") 
}
